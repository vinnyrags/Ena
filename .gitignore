vendor/
!src/Providers/*/assets/**/vendor/
node_modules/

# Compiled assets
dist/

# Lock files
# composer.lock IS tracked — the child theme is the deployed application (not a
# library), so a committed lock gives deterministic `composer install` on deploy.
package-lock.json

# Editor / OS
.idea/
.DS_Store
.phpunit.result.cache
